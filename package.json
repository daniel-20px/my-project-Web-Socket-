{
  "name": "cinedebate",
  "type": "module",
  "version": "1.0.0",
  "description": "Servidor do chat de debates de filmes",
  "main": "server.ts",
  "scripts": {
 "scripts": {
  "dev": "nodemon --exec \"node --experimental-strip-types\" server.ts"
}

}
,
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "nodemon": "^3.1.14",
    "ts-node": "^10.9.2",
    "typescript": "^7.0.2"
  }
}
